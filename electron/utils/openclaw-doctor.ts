// Stub — OpenClaw doctor removed
export async function runOpenClawDoctor(): Promise<{ ok: boolean; checks: unknown[] }> {
  return { ok: true, checks: [] };
}
export async function runOpenClawDoctorFix(): Promise<{ ok: boolean; fixes: unknown[] }> {
  return { ok: true, fixes: [] };
}
