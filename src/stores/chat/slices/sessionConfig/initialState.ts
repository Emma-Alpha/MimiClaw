import type { SessionConfigState } from './types';
export type { SessionConfigState } from './types';

export const initialSessionConfigState: SessionConfigState = {
  sessionModel: null,
  sessionEffort: null,
  sessionThinkingLevel: null,
};
