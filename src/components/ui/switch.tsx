/**
 * Switch — re-exports antd Switch directly.
 * Use antd's native API: onChange(checked, event) instead of onCheckedChange.
 */
export { Switch } from 'antd';
export type { SwitchProps } from 'antd';
