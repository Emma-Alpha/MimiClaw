# Backend env variables are now maintained in the repo-root `.env`.
#
# Preferred setup:
#   cp ../.env.example ../.env
#   # then edit ../.env
#
# Compatibility:
# - `backend/.env` and `backend/.env.local` are still read at startup.
# - Root `.env` / `.env.local` override backend-local values when both exist.
